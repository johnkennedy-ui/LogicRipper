This folder is retained only for the legacy Windows WPF prototype. The primary
MVP GUI is now `src/LogicRipper.Gui.Avalonia`, which runs on Ubuntu.

Business logic still lives in the LogicRipper PowerShell module and is called
through the local CLI bridge.
